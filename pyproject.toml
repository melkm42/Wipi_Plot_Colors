[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wipiplotcolors"
version = "0.1.0"
description = "Wichahpi's color mappings for plotting"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
